---
name: lottery-advanced-analysis
description: 超级大乐透(DLT)与双色球(SSQ)号码高级统计分析引擎。自动增量拉取最新100-200期历史开奖数据（本地CSV持久化缓存），通过可执行Python脚本计算14维基础指标与11项高级数学模型（马尔可夫链、贝叶斯、遗漏回补、蒙特卡洛模拟等），输出下期推荐组合（默认5注单式，支持复式如8+3）及完整分析报告。当用户提到大乐透、双色球、DLT、SSQ、前区后区、红球蓝球、彩票号码分析、号码推荐、复式、走势分析时使用。
---

# 彩票高级综合分析（大乐透 + 双色球）

## 角色与原则

以专业彩票数学分析师身份执行分析。核心原则：

1. 仅基于历史统计与数学模型筛选
2. **每次分析必须基于最新的最少 100 期、推荐 200 期历史数据**（获取方式见下文"数据获取"）
3. **禁止编造历史开奖数据**；数据必须来自脚本拉取或用户提供
4. 两彩种共用同一分析引擎，仅切换参数；**禁止把 DLT 阈值套用到 SSQ，反之亦然**
5. 计算必须通过运行 `scripts/` 中的 Python 脚本完成，禁止心算指标与模型结果

## 彩种路由规则

| 用户输入特征 | 路由结果 |
| --- | --- |
| 提到大乐透 / DLT / 前区后区 | DLT 参数 |
| 提到双色球 / SSQ / 红球蓝球 | SSQ 参数 |
| 要求两个都分析 | 分别跑两套，各出一份报告 |
| 未指明但数据为 5+2 结构 | 默认 DLT |
| 未指明但数据为 6+1 结构 | 默认 SSQ |

## 数据获取（每次分析强制执行，增量更新）

历史数据持久化在 skill 目录的 `data/` 下（`data/dlt_history.csv`、`data/ssq_history.csv`）。
`fetch_data.py` 自带增量逻辑：**CSV 已存在时只拉最新一页数据合并进缓存（同期号新数据覆盖旧数据），缓存断档过大时自动回退全量重拉**——所以每次分析都固定运行同一条命令即可，无需判断缓存状态：

```bash
python scripts/fetch_data.py --game DLT --periods 200 --out data/dlt_history.csv
python scripts/fetch_data.py --game SSQ --periods 200 --out data/ssq_history.csv
```

- **每次分析前必须运行拉取命令**（即使刚拉过——增量模式开销极小，保证数据最新）
- 数据源：DLT 用体彩官方接口（webapi.sporttery.cn），SSQ 用福彩官方接口（cwl.gov.cn），失败自动切换 500.com 备用源
- 脚本输出"新增 X 期 + 期号范围"，向用户转述以确认数据新鲜度
- `--full` 可强制全量重拉（怀疑缓存损坏时使用）
- 所有数据源失败 → 请用户粘贴最新数据（格式：`期号 主区号码... 副区号码...`）
- 有效数据不足 100 期 → 终止分析并说明原因

## 标准执行流程

1. **识别彩种**，确认可选参数：单式注数（默认 5 注）、是否复式（默认否；复式默认 1 组）
2. **增量拉取最新数据**（见上文命令）
3. **运行分析引擎**（内置账本闭环，自动完成"结算上次推荐 → 记录本次推荐"）：

   ```bash
   # 单式（默认5注）
   python scripts/analyze.py --game DLT --data data/dlt_history.csv --out result.json
   # 用户指定注数
   python scripts/analyze.py --game DLT --data data/dlt_history.csv --bets 10 --out result.json
   # 用户要求复式（如"给我推荐个8+3复式"）——只输出复式，默认1组
   python scripts/analyze.py --game DLT --data data/dlt_history.csv --multi 8+3 --out result.json
   # 用户要求多组复式（如"来3组8+3"）
   python scripts/analyze.py --game DLT --data data/dlt_history.csv --multi 8+3 --bets 3 --out result.json
   # 用户要求胆拖（如"2胆6拖"）——只输出胆拖
   python scripts/analyze.py --game DLT --data data/dlt_history.csv --dantuo 2*6 --out result.json
   ```

   脚本一次性完成：数据校验 → 14 维基础指标（冷热判定用威尔逊置信区间）→ 马尔可夫（时间衰减加权）/贝叶斯/遗漏/蒙特卡洛（输出 95% 置信区间）/逆向/爆冷/副区专项模型 → 融合打分（`0.73×基础 + 0.27×高级`）→ 硬过滤 → 组合优选与注间去重
   - **硬过滤默认使用数据驱动校准阈值**（按历史开奖分布自动取分位数，对真实开奖通过率约 66-85%）；原文档固定阈值通过率仅约 7%，会滤掉绝大多数真实开奖形态，仅在用户明确要求时用 `--fixed-filter` 回退。JSON 的 `硬过滤` 字段含生效阈值与通过率对照，报告中须向用户呈现
   - **复式模式（`--multi`）只输出复式号码，不输出单式**；默认 1 组，`--bets N` 变为组数（多组间号码自动差异化）
   - **胆拖模式（`--dantuo 胆*拖`）只输出胆拖**；胆码取评分 Top 且跨三区分布，拖码差异化覆盖，自动算等效注数与金额。与 `--multi` 互斥
   - 其他可选参数：`--mc 200000`（蒙特卡洛次数）、`--seed 42`（可复现）
   - 指标定义与阈值见 [reference/parameters.md](reference/parameters.md)，模型公式见 [reference/models.md](reference/models.md)，硬过滤与评分规则见 [reference/scoring-filter.md](reference/scoring-filter.md)
4. **读取 JSON 结果**，按模板生成中文分析报告 → 模板见 [reference/templates.md](reference/templates.md)

## 输出顺序与号码呈现（硬性要求）

1. **分析内容在前，推荐号码在最后**——报告结构固定为：数据说明 → 基础指标摘要 → 高级模型结果 → 规律总结 → **最后一节是推荐号码**
2. 推荐号码必须放在**独立的代码块（\`\`\`）中**输出纯号码文本（每行一注/一组，格式 `主区号码 + 副区号码`），代码块在终端/界面中可整块选中复制，且不夹杂任何分析文字
3. 号码文本直接取自 JSON 结果的 `号码文本` 字段，禁止手工改写
4. 复式模式下报告只呈现复式（不出现单式推荐），复式号码代码块置于最末，上方标注等效注数与投注金额

## 调用映射

| 用户指令 | 行为 |
| --- | --- |
| 分析大乐透 / 双色球 | 增量拉取 → 全流程 → 默认 5 注单式 |
| 推荐 N 注 | `analyze.py --bets N` |
| 复式 M+S（如 8+3、9+2） | `analyze.py --multi M+S`，**只输出复式（默认 1 组），不输出单式** |
| N 组复式 M+S | `analyze.py --multi M+S --bets N` |
| 胆拖 D 胆 T 拖（如 2 胆 6 拖） | `analyze.py --dantuo D*T`，**只输出胆拖，不输出单式** |
| 对奖 / 核对中奖 | 自动：每次分析已内置结算并播报；手动核对任意号码用 `check.py --tickets ...` |
| 查看历史战绩 / 累计统计 | `ledger.py --game X --stats` |
| 回测 / 验证模型效果 | `backtest.py --game X --data data/xxx.csv --window 100` |
| 自检 / 检查 skill 是否正常 | `selftest.py`（37 项断言；`--offline` 免真实数据） |
| 分析最近 N 期 | `fetch_data.py --periods N`（N≥100）后全流程 |
| 两个彩种都分析 | 分别拉取、分别分析、输出两份报告 |
| 提高蒙特卡洛精度 | `analyze.py --mc 500000` |
| 要求用原文档固定阈值过滤 | `analyze.py --fixed-filter` |
| 用户自带数据 | 跳过拉取，校验后直接 `analyze.py` |

复式合法范围：DLT 主区 6~20 个 + 后区 2~6 个；SSQ 红球 7~24 个 + 蓝球 1~8 个。
胆拖约束：胆码 1~(选号数-1) 个，胆+拖 > 选号数且 ≤ Top池大小。
超范围时脚本报中文错误，向用户转述并请其调整。

## 对奖功能

**自动对奖（默认）**：analyze.py 内置账本闭环，每次分析时自动执行：
1. 结算历史 pending 推荐（目标期已开奖则算命中与奖金，幂等——已结算记录永不重复处理）
2. 落盘本次推荐到 `data/ledger_*.jsonl`（同期同内容不重复记录，与 CSV 增量同思路）
3. JSON 输出 `上次推荐结算`（若有新结算）与 `账本.累计统计`——**报告中若存在 `上次推荐结算` 字段，必须在"数据说明"部分向用户播报上次命中情况**
- `--no-ledger` 可关闭闭环

**手动对奖**：用户拿任意号码问"中了没"时：
1. 先运行 `fetch_data.py` 确保开奖数据已更新到目标期
2. 运行 `python scripts/check.py --game DLT --tickets "号码文本或文件路径" [--period 期号]`

**账本管理**：`python scripts/ledger.py --game DLT --settle`（手动结算）、`--stats`（累计统计：投入/回收/回报率/随机基线对照）。若数据窗口滚过了推荐期号导致无法自动结算，记录会标记为 expired，可用 `check.py --period 期号` 手动核对。

## 回测与诚实性

`backtest.py` 用滚动窗口验证评分模型 vs 随机基线。已验证结论（DLT 200 期数据）：**评分模型的号码覆盖能力与随机选号无统计学显著差异（提升 +2.6%，z=0.67）**——这符合彩票独立随机的数学本质。因此：
- 向用户解释模型价值时，定位是**形态筛选与资金结构管理工具**（复式/胆拖优化、避开极端形态），而非提高中奖概率
- 用户若质疑模型有效性，可主动运行回测并展示结果，不回避
- **账本是诚实账目，不是学习信号**：累计命中/回报数据仅用于与随机基线对照展示，**严禁**根据账本命中记录调整评分权重或模型参数——对独立随机事件这样做是过拟合噪声。唯一合法的数据驱动更新是硬过滤校准阈值随新数据自动刷新（analyze.py 已内置）
- 若用户要求"根据历史命中优化模型"，应解释上述原理并拒绝改动权重，可展示账本回报率 vs 随机基线（约 0.27）作为佐证

## 约束与禁止（硬性）

1. 禁止声称必中、稳赚、准确预测
2. 禁止编造历史开奖数据；数据必须来自脚本拉取或用户提供
3. 每次分析必须先运行增量拉取，且基于最新的 ≥100 期数据；数据不足时终止并说明
4. 禁止跳过脚本、凭模型直觉输出指标数值或推荐组合
5. 推荐号码必须合法且不重复（DLT 前区 ∈01-35 + 后区 ∈01-12；SSQ 红球 ∈01-33 + 蓝球 ∈01-16）
6. 推荐组合必须通过硬过滤，除非用户明确关闭
7. 禁止跨彩种混用阈值参数
8. 推荐号码必须以纯号码代码块置于输出最末，前面不得再有其他号码性内容
